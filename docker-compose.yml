services:
  api:
    build: .
    ports:
      - "3000:3000"

    environment:
      PORT: 3000
      MONGO_URI: mongodb+srv://wrpw:2541@cluster0.6pedetw.mongodb.net/shopDB
      JWT_REFRESH_SECRET: superrefreshsecret

    depends_on:
      - mongo

  mongo:
    image: mongo

    ports:
      - "27017:27017"

    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
